# Run-With-It Progress — UX + Polish Bundle (PRD #32)

> Branch: `feat/ux-polish-bundle` (off `main`)
> PRD: https://github.com/Splitzzyy/Portivio/issues/32
> Started: 2026-05-08

This file tracks slice-by-slice execution so a session can be resumed after a token-budget cut-off. Read it top-to-bottom; the first slice with `status != done` is the next one to work.

---

## Routing decisions

Sequential execution (single coordinator, direct implementation — runner CLI bypassed; gemini argv quirk + Windows env).

| Slice | Issue | Score | Level | Capability | Agent | Status | Commit |
|-------|-------|-------|-------|-----------|-------|--------|--------|
| A | #33 — FD/RD AccountNo optional | 11 | quite-easy | fast | claude-direct | done | 65f1514 |
| B | #34 — Tx paged-envelope fix | 12 | quite-easy | fast | claude-direct | done | aa88bb8 |
| C | #35 — Gold freshness (manual + startup) | 15 | easy | fast | claude-direct | done | 90b02e6 |
| D | #36 — Design tokens + ThemeService | 20 | medium | balanced | claude-direct | done | eb4a968 |
| E | #37 — Holdings + Tx polish (blocked by D) | 17 | easy | fast | claude-direct | done | 4585835 |
| F | #38 — Add Investment + shell + mobile (blocked by D, E) | 27 | medium-hard | advanced | claude-direct | done | (pending commit) |

`ROUTE` lines emitted per slice as work begins.

---

## How to resume

If this session was cut off, the next session can:

1. Read this file. Find the first row with `status != done`.
2. `git status -sb` — confirm current branch is `feat/ux-polish-bundle` and HEAD matches the last `Commit` shown above.
3. Read the corresponding slice issue body on GitHub: `gh issue view <issue#>`.
4. Pick up where the previous session left off — implement the slice's acceptance criteria, run tests, commit, update this file's row.
5. Continue down the table.

Open PR after slice F (or earlier if user requests partial ship).

---

## Per-slice notes

### Slice A — #33 FD/RD AccountNo optional

- Files: `AssetInstrumentService.cs`, `AssetRequests.cs`, `AssetInstrumentServiceTests.cs`, `add-investment.component.ts/.html`.
- Tests required: 3 backend cases listed in the issue body.
- Verification: `dotnet build`, full backend xUnit suite, manual POST.

### Slice B — #34 Transactions paged-envelope fix

- Files: `transaction.service.ts`, `portfolio.model.ts` (add `PagedResult<T>`), `transactions.component.ts/.html`, new `transactions.component.spec.ts`, `add-investment.component.ts` (fix recentTransactions destructure).
- Tests: 3 Karma specs.
- Verification: `bun run test --include='**/transactions.component.spec.ts'`, manual visit.

### Slice C — #35 Gold freshness

- Files: `HoldingRecalculationService.cs` (extend `RefreshProfileAsync`), `Program.cs` (startup enqueue).
- Tests: 2 backend cases.
- Verification: `dotnet build`, manual click `Refresh prices` on a profile with gold.

### Slice D — #36 Design tokens + ThemeService

- Files: new `_tokens.scss` partial, edit `styles.scss` + `shared-page.scss`, new `core/services/theme.service.ts` + spec, top-bar wiring (`app.component.html` or dashboard layout).
- Tokens: ~10 CSS custom properties (canvas/surface/text/muted/border/accent/positive/negative).
- Tests: 4 Karma specs on ThemeService.
- Verification: `bun run prod`, manual cycle of toggle.

### Slice E — #37 Holdings + Tx polish

- Files: per-page SCSS for Holdings + Transactions, edit `shared-page.scss` for `.page-header` flex, asset-pill class helper in `holdings.component.ts`.
- Verification: visit at 1440 / 1024 / 768 / 480 / 375 in both themes.

### Slice F — #38 Add Investment + shell + mobile

- Files: `add-investment.component.html/.scss`, `app.component.html/.scss`, sidebar component, every page's responsive rules.
- Verification: walk every dashboard page at 5 viewports × 2 themes.

---

## Status legend

- `pending` — not started.
- `in-progress` — currently being worked on; partial commits may exist on disk but not yet pushed.
- `done` — committed locally; tests pass.
- `pushed` — branch pushed to origin.
- `merged` — landed in main.
